services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./tasks.db:/app/tasks.db
      - ./checkpoints.db:/app/checkpoints.db
    restart: unless-stopped
